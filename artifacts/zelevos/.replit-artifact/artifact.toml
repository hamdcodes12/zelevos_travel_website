kind = "web"
previewPath = "/"
title = "Zelevos AI Travel OS"
description = "An AI travel operating system that turns travel intent into a coordinated trip with planning, booking, monitoring, marketplace discovery, wallet, and concierge support."
version = "1.0.0"
id = "artifacts/wayora"
router = "path"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 19313

[services.development]
run = "pnpm --filter @workspace/wayora run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/wayora", "run", "build" ]
serve = "static"
publicDir = "artifacts/wayora/dist/public"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "19313"
BASE_PATH = "/"
